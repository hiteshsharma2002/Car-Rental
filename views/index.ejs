<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-color: #f0f0f0;
        }

        h1 {
            color: #333;
            margin-bottom: 20px;
        }

        a button {
            padding: 10px 20px;
            font-size: 16px;
            color: white;
            background-color: #007bff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        a button:hover {
            background-color: #0056b3;
        }

        a {
            text-decoration: none;
        }

        button {
            outline: none;
        }

        .container {
            text-align: center;
            margin-bottom: 40px;
        }

        .container:last-child {
            margin-bottom: 0;
        }

        br {
            display: none;
        }

        @media (max-width: 600px) {
            a button {
                font-size: 14px;
                padding: 8px 16px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>User Information</h1>
        <a href="/user/userLogin"><button>Login</button></a>
        <a href="/user/userSignup"><button>New User</button></a>
    </div>

    <div class="container">
        <h1>Admin</h1>
        <a href="/admin/adminLogin"><button>Login</button></a>
    </div>
</body>
</html>
